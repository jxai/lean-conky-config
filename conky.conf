-- vim: ts=4 sw=4 noet ai cindent ft=lua

-- NOTE: Do NOT modify this file for local customization. Instead, create a
--       `local.conf` file from `local.conf.example` and make changes there.

local _dirname_ = debug.getinfo(1, 'S').source:sub(2):match('(.*[/\\])')

conky.config = {
    alignment = 'top_right',
    background = false,
    border_inner_margin = 8,
    border_outer_margin = 0,
    border_width = 1,
    color0 = 'white',  -- clock color
    color1 = 'white',  -- heading 1 color
    color2 = 'white',  -- heading 2 color
    color3 = 'lightgray',  -- chart color
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = '#555',
    default_shade_color = '#555',
    diskio_avg_samples = 2,
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = true,
    draw_shades = false,
    extra_newline = false,
    font = 'sans-serif:normal:size=8',
    gap_x = 8,
    gap_y = 32,
    lua_load = _dirname_ .. 'main.lua',
    maximum_width = 270,
    minimum_height = 320,
    minimum_width = 270,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_stderr = false,
    override_utf8_locale = true,
    own_window = true,
    own_window_argb_value = 128,
    own_window_argb_visual = true,
    own_window_class = 'Conky',
    own_window_transparent = false,
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 2,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
    xinerama_head = 1,
}

conky.text = [[
${color0}${voffset 2}${font}${time %A %B %d, %Y}
${voffset 4}${font :bold:size=18}${time %H:%M:%S}${color}
${color1}${lua font icon { ${voffset -4}} {}}${font :bold:size=10}SYSTEM ${hr 1}${color}
${font}${sysname} ${kernel} ${alignr}${machine}
Host:${alignr}${nodename}
Uptime:${alignr}${uptime}
Processes:${alignr}${running_processes} / ${processes}

${color1}${lua font icon { ${voffset -4}} {}}${font :bold:size=10}CPU ${hr 1}${color}
${font}${execi 3600 grep model /proc/cpuinfo | cut -d : -f2 | tail -1 | sed 's/\s//'} ${alignr} ${cpu cpu0}%
${color3}${cpugraph cpu0 32,270}${color}
${lua top_cpu_line header}
${lua top_cpu_line 1}
${lua top_cpu_line 2}
${lua top_cpu_line 3}
${lua top_cpu_line 4}
${lua top_cpu_line 5}

${color1}${lua font icon { ${voffset -4}} {}}${font :bold:size=10}MEMORY ${hr 1}${color}
${color2}${font :bold:size=8}RAM${font}${color} ${alignc -16}${mem} / ${memmax} ${alignr}${memperc}%
${color3}${membar 4}${color}
${color2}${font :bold:size=8}SWAP${font}${color} ${alignc -16}${swap} / ${swapmax} ${alignr}${swapperc}%
${color3}${swapbar 4}${color}
${lua top_mem_line header}
${lua top_mem_line 1}
${lua top_mem_line 2}
${lua top_mem_line 3}
${lua top_mem_line 4}
${lua top_mem_line 5}

${color1}${lua font icon { ${voffset -4}} {}}${font :bold:size=10}DISK USAGE ${hr 1}${color}
${lua disks 5}
${font}Read: ${diskio_read} ${alignr}Write: ${diskio_write}
${color3}${diskiograph_read 32,130} ${alignr}${diskiograph_write 32,130}${color}
${lua top_io_line header}
${lua top_io_line 1}
${lua top_io_line 2}
${lua top_io_line 3}
${lua top_io_line 4}
${lua top_io_line 5}

${color1}${lua font icon { ${voffset -4}} {}}${font :bold:size=10}NETWORK ${hr 1}${color}
${color2}${font :bold:size=8}Local IPs:${alignr}External IP:${font}${color}
${execi 60 ip a | grep inet | grep -vw lo | grep -v inet6 | cut -d \/ -f1 | sed 's/[^0-9\.]*//g'}  ${alignr}${texeci 3600  wget -q -O- https://ipecho.net/plain; echo}
${lua ifaces 10}]]


-- font definitions
conky.fonts = {
    icon = 'FontAwesome:size=12',  -- icons
    h1 = ':bold:size=10',  -- heading 1
    h2 = ':bold:size=8',  -- heading 2
}

-- process local config
local function _load_local_config()
    dofile(_dirname_ .. 'local.conf')
end

if pcall(_load_local_config) then
    for k, v in pairs(local_config) do conky.config[k] = v end
    for k, v in pairs(local_fonts) do conky.fonts[k] = v end
end
